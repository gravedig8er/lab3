#include <iostream> 
#include <fstream> 
#include "ForwardList.h"
#include "String.h"
#include "MyFunctions.h"

int main() {
  List lst; // список из строк 
  std::fstream input, out; 

  lst.SetLength(GetFileSize(input));
  return 0;
}
